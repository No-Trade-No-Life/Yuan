## API Report File for "@yuants/time-series"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export const combine: <T>(tags: Record<string, string>, mapper: (index: number, self: ITimeSeries<T>) => T, sources: ITimeSeries<any>[]) => ITimeSeries<T>;

// @public
export const EMA: (source: ITimeSeries<number>, length: number) => ITimeSeries<number>;

// @public
export interface ITimeFrame {
    // (undocumented)
    commit(): void;
    // (undocumented)
    createTimeSeries<T>(tags: Record<string, string>, onCalc?: () => void): ITimeSeries<T>;
    // (undocumented)
    readonly list: ReadonlyArray<ITimeSeries<any>>;
    // (undocumented)
    readonly time: ITimeSeries<number>;
}

// @public
export interface ITimeSeries<T> extends ReadonlyArray<T> {
    // (undocumented)
    [index: number]: T;
    // (undocumented)
    calc(): void;
    // (undocumented)
    cleanLength(): number;
    // (undocumented)
    commit(): void;
    // (undocumented)
    tags: Record<string, string>;
    // (undocumented)
    readonly timeFrame: ITimeFrame;
}

// @public
export const scan: <T>(tags: Record<string, string>, init: () => T, reducer: (acc: T, index: number, self: ITimeSeries<T>) => T, sources: ITimeSeries<any>[]) => ITimeSeries<T>;

// @public
export const SMA: (source: ITimeSeries<number>, length: number) => ITimeSeries<number>;

// @public
export class TimeFrame implements ITimeFrame {
    constructor();
    // (undocumented)
    commit(): void;
    // (undocumented)
    createTimeSeries<T>(tags: Record<string, string>, onCalc?: () => void): ITimeSeries<T>;
    // (undocumented)
    list: readonly ITimeSeries<any>[];
    // (undocumented)
    time: ITimeSeries<number>;
}

// (No @packageDocumentation comment for this package)

```
