## 目标

- 只读分析当前 `http-proxy` 相关活跃任务，并准确说明 `TRUSTED_HTTP_PROXY_TERMINAL_IDS` 的职责、调用链和默认行为。

## 推荐做法

1. 先以 `legionmind` 恢复当前活跃任务，确认 `vendor-tokenbucket-proxy-ip` 是当前与 `http-proxy` 最相关的任务，重点关注“`bucketKey` 与 `labels.ip` 同源”的设计目标。
2. 追踪 `TRUSTED_HTTP_PROXY_TERMINAL_IDS` 在 `libraries/http-services/src/proxy-ip.ts` 中的解析与消费路径，确认它过滤的是 `terminal_id` 白名单，而不是 IP 或 service id。
3. 串起完整运行链路：`apps/http-proxy/src/index.ts` 注入可信 `ip/ip_source` -> `libraries/http-services/src/server.ts` 用 `labels` 建路由约束 -> `libraries/http-services/src/client.ts` 下发 `labels` -> `apps/vendor-binance/src/api/client.ts` 在代理模式消费 `{ ip, terminalId, bucketKey }`。
4. 用 `.legion` walkthrough、安全审查和单测交叉验证结论，确认该变量属于 trust boundary 配置，且未配置时是 fail-closed，而不是自动放行。
5. 最终给用户一份结论：说明当前任务背景、该环境变量的精确定义、它影响的行为、以及空配置/不可信终端时的报错与回退表现。

## 关键文件

- `libraries/http-services/src/proxy-ip.ts`
- `apps/http-proxy/src/index.ts`
- `libraries/http-services/src/client.ts`
- `libraries/http-services/src/server.ts`
- `apps/vendor-binance/src/api/client.ts`
- `libraries/http-services/src/__tests__/proxy-ip.test.ts`
- `.legion/tasks/vendor-tokenbucket-proxy-ip/plan.md`
- `.legion/tasks/vendor-tokenbucket-proxy-ip/docs/report-walkthrough-impl.md`
- `.legion/tasks/vendor-tokenbucket-proxy-ip/docs/review-security.md`

## 验证

- 确认 `getTrustedProxyTerminalIds()` 仅把 `TRUSTED_HTTP_PROXY_TERMINAL_IDS` 解析为逗号分隔的 `terminal_id` 集合。
- 确认只有同时满足“在 allowlist 中 + `ip_source === 'http-services'` + 合法 `tags.ip` + 提供 `HTTPProxy` 服务”的终端才会进入候选池。
- 确认 `TRUSTED_HTTP_PROXY_TERMINAL_IDS` 为空时，`rebuildProxyIpCache()` 记录 deny-all 日志，`acquireProxyBucket()` / `waitForHTTPProxyIps()` 最终表现为 `E_PROXY_TARGET_NOT_FOUND`。
- 确认 `apps/http-proxy/src/index.ts` 只有在来源可信时才注册 `labels.ip`，避免不可信标签参与路由。
- 确认 `apps/vendor-binance/src/api/client.ts` 在代理模式下把 `{ ip, terminal_id }` 带入 `labels`，使请求路由和 per-ip token bucket 保持一致。

## 备注

- 本轮是分析说明，不计划修改业务代码。
